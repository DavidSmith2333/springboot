<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xiaoliu.mapper.ReportMapper">

    <select id="getAll" resultType="Report">
        select id,reportType,reportTitle,reportDate
        from springbootweb.report
    </select>


    <select id="getReportById" resultType="Report">
        select id,reportType,reportTitle,reportDate
        from springbootweb.report sbwr
        where sbwr.id = #{id}
    </select>

    <delete id="delete">
        delete from springbootweb.report where id = #{id}
    </delete>

    <insert id="save" parameterType="Report">
        insert into springbootweb.report (reportType, reportTitle, reportDate)
        VALUES (#{reportType}, #{reportTitle}, #{reportDate})
    </insert>

    <update id="updateRepById" parameterType="Report">
        update springbootweb.report set reportType = #{reportType},
        reportTitle = #{reportTitle},
        reportDate = #{reportDate}
        where id = #{id}
    </update>


</mapper>
